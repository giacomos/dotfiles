[user]
	name = Giacomo Spettoli
	email = giacomo.spettoli@gmail.com
[credential]
	helper = cache --timeout 0
[color "status"]
  changed = yellow
  added = green
  untracked = blue
[push]
	default = simple
[http]
	sslVerify = false
[color]
    branch = auto
    diff = auto
    interactive = auto
    status = auto
[branch]
    autosetupmerge = true
[alias]
    st = status -sb
    ci = commit -v
    br = branch
    co = checkout
    df = diff
    dc = diff --cached
    hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
    ls = ls-tree --full-tree -r HEAD
    m = merge --no-ff
    st = status
    ai = add -i
    ap = add -p
    d = diff
    r = remote
    dc = diff --cached
    ci = commit -v
    cia = commit -v -a
    cp = cherry-pick
    l = log
    ll = log -p
    b = branch
    reha = reset --hard
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%an - %cr)%Creset' --abbrev-commit --date=relative
    p = pull
    f = fetch
[include]
path = .gitconfig.user
[core]
	excludesfile = /home/jack/.gitignore
